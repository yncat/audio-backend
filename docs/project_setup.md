# プロジェクトセットアップ
MSVC のプロジェクトを作り、コマンドラインからも Visual Studio からもビルドできるような環境を整える。
まずは audiobackend というプロジェクトを作成
src ディレクトリに main.cpp を配置
ここには、 dll エントリーポイントをとりあえず記述
64bit dll としてビルドされるように設定を整える
ビルドコマンドを readme.md に書く
実際にビルドできることを確かめる

# サンプルプログラムのひな形を作る
audiobackend_examples というプロジェクトを作る
これは、 audiobackend.dll を使うサンプルプログラムで、シンプルなCUIアプリケーションとして実装する。
サンプルプログラムを起動したら、数字を入力して選ぶメニューを表示する。とりあえず今は 0: quit だけでよい。将来的にここにいろんな項目を増やして、新機能を試せるようにしていく。
64bit application としてビルドされるように設定を調整。
ビルドコマンドを readme.md に書く。
exeファイルがビルドできることを確かめる。

# revision 2
今はバッチファイルでのビルドになっているため、ファイルが追加されるたびにビルドスクリプトに手を入れなければならず、煩雑である。
そのため、 nmake でビルドできるようにプロジェクト構成を変更。

